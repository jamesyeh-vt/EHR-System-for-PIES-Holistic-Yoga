import SimpleTablePage from "../../components/SimpleTablePage";

export default function AssignedClientsPage() {
  // replace with API data later
  const rows = ["Jane Doe", "John Smith"];
  return <SimpleTablePage rows={rows} />;
}